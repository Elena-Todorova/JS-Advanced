function daysInMonth(month, year) {
   let day = params;

  // day.setMonth(day.getMonth() + 1);
  // day.setDate(0);
console.log(day);
    //console.log(day.getGate());
}

daysInMonth(1, 2012)
daysInMonth(2, 2021)